name: Schedule a Workflow

on:
  schedule:
    - cron: "*/1 * * * *"
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a script
        run: echo "This is a scheduled event"
